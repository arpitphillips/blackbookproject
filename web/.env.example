# Copy to .env.local for local development. These are PUBLIC (anon) values that
# ship to the browser — never put the Supabase service-role key here.
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key
